# Secrets - niemals committen
config.py

# Python
__pycache__/
*.pyc
.venv/
thermo-env/

# Editor/OS
.DS_Store

# Settings-Datei (falls sie mal versehentlich projektlokal statt in
# STATE_DIR landet, z.B. bei einem lokalen Testlauf ohne config.STATE_DIR)
settings.json
settings.json.tmp

# Fernet key for encrypted system-report SSH passwords (secrets_crypto.py) -
# same "shouldn't happen outside STATE_DIR" reasoning as settings.json above
secret.key

# Claude Code
.claude/

# Local development
.venv-lint/
.ruff_cache/

# Claude Code local instructions
CLAUDE.md
